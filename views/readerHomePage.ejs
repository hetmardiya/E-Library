<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reader Profile - Libretto</title>
    <style>
        :root {
            --primary: #2c5282;
            --secondary: #f8f9fa;
            --accent: #805ad5;
            --text: #2d3748;
            --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
            line-height: 1.6;
        }

        .header {
            background: linear-gradient(135deg, var(--primary) 0%, #1a365d 100%);
            padding: 1.5rem 0;
            color: white;
        }

        /* Navbar styles */
        .nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 2rem;
            max-width: 1400px;
            margin: 0 auto;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
        }

        .nav-links a {
            color: white;
            text-decoration: none;
            padding: 0.5rem 1rem;
            border-radius: 25px;
            transition: all 0.3s ease;
        }

        .nav-links a:hover {
            background: rgba(255, 255, 255, 0.1);
            transform: translateY(-2px);
        }

        /* Book slider styles */
        .purchased-books {
            margin-top: 3rem;
            padding: 2rem 0;
        }

        .books-slider {
            position: relative;
            overflow-x: auto;
            padding: 1rem 0;
            scrollbar-width: none;
            -ms-overflow-style: none;
        }

        .books-slider::-webkit-scrollbar {
            display: none;
        }

        .books-row {
            display: flex;
            gap: 2rem;
            padding: 0 1rem;
        }

        .purchased-book {
            min-width: 160px;
            text-align: center;
            transition: transform 0.3s ease;
        }

        .purchased-book:hover {
            transform: translateY(-10px);
        }

        .book-cover-img {
            width: 160px;
            height: 220px;
            background: linear-gradient(135deg, #e9d8fd 0%, #d6bcfa 100%);
            border-radius: 8px;
            box-shadow: var(--shadow);
            margin-bottom: 0.5rem;
        }

        .purchased-book h4 {
            font-size: 0.9rem;
            margin: 0.5rem 0;
            color: var(--text);
        }

        .purchased-book p {
            font-size: 0.8rem;
            color: #666;
        }

        .scroll-btn {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            background: white;
            border: none;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            box-shadow: var(--shadow);
            cursor: pointer;
            z-index: 10;
        }

        .scroll-left {
            left: 0;
        }

        .scroll-right {
            right: 0;
        }

        .scroll-btn:hover {
            background: var(--secondary);
        }

        /* Footer styles */
        .footer {
            background: var(--primary);
            color: white;
            padding: 3rem 0;
            margin-top: 4rem;
        }

        .footer-content {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 3rem;
            padding: 0 2rem;
        }

        .footer-section h3 {
            margin-bottom: 1.5rem;
            color: #e2e8f0;
        }

        .footer-links {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 0.8rem;
        }

        .footer-links a {
            color: #a0aec0;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .footer-links a:hover {
            color: white;
        }

        .footer-bottom {
            text-align: center;
            padding-top: 2rem;
            margin-top: 2rem;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        @media (max-width: 768px) {
            .nav {
                padding: 0 1rem;
            }

            .nav-links {
                gap: 1rem;
            }

            .footer-content {
                grid-template-columns: 1fr;
                padding: 0 1rem;
            }
        }

        /* Add back these styles after the basic styles */
        .reader-profile {
            display: flex;
            align-items: center;
            gap: 2rem;
            margin-bottom: 2rem;
        }

        .reader-image {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            border: 4px solid var(--accent);
            object-fit: cover;
        }

        /* Reading Progress Section */
        .reading-progress {
            background: white;
            border-radius: 12px;
            padding: 1.5rem;
            margin-top: 2rem;
            box-shadow: var(--shadow);
        }

        .progress-bar {
            width: 100%;
            height: 12px;
            background: #e2e8f0;
            border-radius: 6px;
            margin: 1rem 0;
            overflow: hidden;
        }

        .progress-fill {
            height: 100%;
            background: var(--progress);
            width: 65%;
            border-radius: 6px;
            transition: width 0.3s ease;
        }

        /* Achievement Section */
        .achievements-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 1rem;
            margin-top: 2rem;
        }

        .achievement-card {
            background: white;
            padding: 1rem;
            border-radius: 12px;
            text-align: center;
            box-shadow: var(--shadow);
            border-top: 4px solid var(--accent);
        }

        .achievement-icon {
            font-size: 2rem;
            color: var(--accent);
            margin-bottom: 0.5rem;
        }

        /* Reading Goal Section */
        .reading-goal {
            background: white;
            border-radius: 12px;
            padding: 1.5rem;
            margin-top: 2rem;
            box-shadow: var(--shadow);
            text-align: center;
        }

        .goal-progress {
            font-size: 2.5rem;
            color: var(--accent);
            font-weight: bold;
            margin: 1rem 0;
        }

        /* Currently Reading Section */
        .currently-reading {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-top: 2rem;
        }

        .book-item {
            background: white;
            border-radius: 12px;
            padding: 1rem;
            box-shadow: var(--shadow);
            display: flex;
            gap: 1rem;
            transition: transform 0.3s ease;
        }

        .book-item:hover {
            transform: translateY(-5px);
        }

        .book-thumbnail {
            width: 80px;
            height: 120px;
            background: linear-gradient(135deg, #e9d8fd 0%, #d6bcfa 100%);
            border-radius: 6px;
        }

        /* Update media queries */
        @media (max-width: 768px) {
            .reader-profile {
                flex-direction: column;
                text-align: center;
            }

            .currently-reading {
                grid-template-columns: 1fr;
            }

            .achievements-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        /* Add these styles in the style section */

        /* Reading Lists Section */
        .reading-lists-section {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin: 3rem 0;
            padding: 0 2rem;
        }

        .list-card {
            background: white;
            border-radius: 15px;
            padding: 1.5rem;
            box-shadow: var(--shadow);
            transition: all 0.3s ease;
            border-top: 4px solid var(--accent);
        }

        .list-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
        }

        .list-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 1.5rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid var(--secondary);
        }

        .list-header h3 {
            color: var(--primary);
            font-size: 1.3rem;
            margin-bottom: 0.3rem;
        }

        .list-header p {
            color: #666;
            font-size: 0.9rem;
        }

        .list-icon {
            width: 50px;
            height: 50px;
            background: var(--secondary);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            color: var(--accent);
            transition: transform 0.3s ease;
        }

        .list-card:hover .list-icon {
            transform: scale(1.1);
        }

        .list-books {
            margin: 1rem 0;
        }

        .list-book-item {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 0.8rem 0;
            border-bottom: 1px solid var(--secondary);
            transition: transform 0.2s ease;
        }

        .list-book-item:hover {
            transform: translateX(10px);
        }

        .list-book-item:last-child {
            border-bottom: none;
        }

        .list-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 1.5rem;
            padding-top: 1rem;
            border-top: 2px solid var(--secondary);
        }

        .list-stats {
            color: #666;
            font-size: 0.9rem;
        }

        .view-all-btn {
            background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
            color: white;
            padding: 0.5rem 1.2rem;
            border: none;
            border-radius: 25px;
            font-size: 0.9rem;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .view-all-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(44, 82, 130, 0.2);
        }

        @media (max-width: 768px) {
            .reading-lists-section {
                grid-template-columns: 1fr;
                padding: 0 1rem;
            }

            .list-card {
                padding: 1rem;
            }
        }
    </style>
</head>

<body>
    <header class="header">
        <nav class="nav container">
            <a href="index.html" style="color: white; text-decoration: none;">
                <h2>Libretto</h2>
            </a>
            <div class="nav-links">
                <a href="#library">My Library</a>
                <a href="#lists">Reading Lists</a>
                <a href="#goals">Goals</a>
                <a href="#settings">Settings</a>
            </div>
        </nav>
        <div class="container">
            <div class="reader-profile">
                <img src="reader-placeholder.jpg" alt="Reader Name" class="reader-image">
                <div class="reader-info">
                    <h1>Alex Thompson</h1>
                    <p>Avid Reader | Fantasy Enthusiast | Book Reviewer</p>
                </div>
            </div>
        </div>
    </header>

    <main class="container">

        <section class="purchased-books container">
            <h2>Your Library</h2>
            <div class="books-slider">
                <button class="scroll-btn scroll-left">←</button>
                <button class="scroll-btn scroll-right">→</button>
                <div class="books-row">
                    <div class="purchased-book">
                        <div class="book-cover-img"></div>
                        <h4>The Great Gatsby</h4>
                        <p>F. Scott Fitzgerald</p>
                    </div>
                    <div class="purchased-book">
                        <div class="book-cover-img"></div>
                        <h4>1984</h4>
                        <p>George Orwell</p>
                    </div>
                    <div class="purchased-book">
                        <div class="book-cover-img"></div>
                        <h4>Pride and Prejudice</h4>
                        <p>Jane Austen</p>
                    </div>
                    <div class="purchased-book">
                        <div class="book-cover-img"></div>
                        <h4>The Hobbit</h4>
                        <p>J.R.R. Tolkien</p>
                    </div>
                    <div class="purchased-book">
                        <div class="book-cover-img"></div>
                        <h4>Dune</h4>
                        <p>Frank Herbert</p>
                    </div>
                    <div class="purchased-book">
                        <div class="book-cover-img"></div>
                        <h4>The Alchemist</h4>
                        <p>Paulo Coelho</p>
                    </div>
                </div>
            </div>
        </section>
        <section class="reading-lists-section">

            <div class="list-card">
                <div class="list-header">
                    <div>
                        <h3>Want to Read</h3>
                        <p>Your reading wishlist</p>
                    </div>
                    <div class="list-icon">📖</div>
                </div>
                <div class="list-books">
                    <!-- Similar book items -->
                </div>
                <div class="list-footer">
                    <div class="list-stats">23 books in list</div>
                    <button class="view-all-btn">View All</button>
                </div>
            </div>

            <div class="list-card">
                <div class="list-header">
                    <div>
                        <h3>favrites</h3>
                        <p>favrites books</p>
                    </div>
                    <div class="list-icon">⭐</div>
                </div>
                <div class="list-books">
                    <!-- Similar book items -->
                </div>
                <div class="list-footer">
                    <div class="list-stats">156 books completed</div>
                    <button class="view-all-btn">View All</button>
                </div>
            </div>
        </section>
    </main>

    <footer class="footer">
        <div class="footer-content">
            <div class="footer-section">
                <h3>Quick Links</h3>
                <ul class="footer-links">
                    <li><a href="#library">My Library</a></li>
                    <li><a href="#lists">Reading Lists</a></li>
                    <li><a href="#goals">Reading Goals</a></li>
                    <li><a href="#achievements">Achievements</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h3>Support</h3>
                <ul class="footer-links">
                    <li><a href="#help">Help Center</a></li>
                    <li><a href="#contact">Contact Us</a></li>
                    <li><a href="#feedback">Give Feedback</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h3>Community</h3>
                <ul class="footer-links">
                    <li><a href="#discussions">Book Discussions</a></li>
                    <li><a href="#reviews">Your Reviews</a></li>
                    <li><a href="#groups">Reading Groups</a></li>
                </ul>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; 2023 Libretto. All rights reserved.</p>
        </div>
    </footer>

    <script>
        // Existing slider script (if any)...

        // Add this new script for horizontal book slider
        document.addEventListener('DOMContentLoaded', function () {
            const slider = document.querySelector('.books-slider');
            const leftBtn = document.querySelector('.scroll-left');
            const rightBtn = document.querySelector('.scroll-right');

            leftBtn.addEventListener('click', () => {
                slider.scrollBy({
                    left: -200,
                    behavior: 'smooth'
                });
            });

            rightBtn.addEventListener('click', () => {
                slider.scrollBy({
                    left: 200,
                    behavior: 'smooth'
                });
            });
        });
    </script>
</body>

</html>